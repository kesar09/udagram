version: "3"
services:
  reverseproxy:
    build:
      context: .
    image: kesar09/udagram-deployment
  backend_user:
    build:
      context: ../../udagram-api-user
    image: kesar09/udagram-api-user
  backend_feed:
    build:
      context: ../../udagram-api-feed  
    image: kesar09/udagram-api-feed
  frontend:
    build:
      context: ../../udagram-frontend  
    image: kesar09/udagram-frontend
